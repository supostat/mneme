

# dev-workflow (session data)
.dev-vault/daily/
.dev-vault/branches/
.dev-vault/.intelligence.json
# node / bun
node_modules/
*.tsbuildinfo
# bun build --compile leaves temp files in cwd (repo root) when scripts/build-plugin.ts runs
*.bun-build
# release artifacts (reproducible from source via scripts/build-release.ts)
dist-release/
# benchmark datasets (fetched by scripts/bench/download.ts)
scripts/bench/datasets/

# dev-workflow (stack-detected)
.DS_Store
*.swp
*.swo
*~
coverage/
.env
.env.*
.claude
.dev-vault
.vscode
.mcp.json
CLAUDE.md
.engram/
docs/*
!docs/METRICS.md
!docs/BENCH.md
# The test suite reads this one (tests/handoff-used-notes.test.ts pins the engine's contract texts
# against it), so it must exist in a fresh clone — the same reason METRICS.md is tracked.
!docs/HANDOFF-USED-NOTES.md
# Same reason: tests/handoff-survey.test.ts pins the survey tool's names against it.
!docs/HANDOFF-SURVEY.md

# analytics package (generated, local-only)
mneme-analytics/
